#!/usr/bin/node

const factList = require('./100-data').list;
const factArr = factList.map(factFunc);

function factFunc (currentValue, index) {
  return currentValue * index;
}
console.log('abc' + factList + factArr);
console.log(`${factList}\n${factArr}`);
